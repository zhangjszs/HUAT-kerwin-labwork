哪个符号加在命令后面可以在后台执行程序 （&）
关于shell脚本中的函数的说法，正确的是 （以上各选项都正确）
下面哪条命令可用来使shell变量变为一个全局变量 （export）
当字符串用单引号括起来时，shell将 （不解释引号内的特殊字符）
假如在脚本中定义了如下的变量: var="hello Linux" 那么下列哪个选项可以成功打印变量var的值itheima? （echo "$var"）
下列对shell变量FRUIT操作，正确的是 （显示变量的值:echo $FRUIT）
下列哪个选项可以测试file是否是普通文件? （test [ -f file ]）
在Linux shell中，下面哪个变量代表的是shell程序命令的程序文件名 （$0）
下列变量名中有效的shell变量名是 （trust_no_1）
下列哪个选项不能将标准输出重定向到file文件中? （cat /etc/passwd 0>file）
若需要在当前终端上顺序执行命令，则应使用下列哪个符号连接? （;）
若要将如下命令合并为一个命令: ls -l /etc >tmp.txt grep init < tmp.txt 可使用哪个符号连接? （|）
Linux命令行是由 （shell） 提供的。
假如在脚本中定义了如下的变量: var="hello Tom" 那么下列哪个选项可以成功打印变量var的值Tom? （echo "$var"）
一个bash shell脚本的第一行是 （#!/bin/bash）
下列哪个选项是用于条件判断的命令? （test）
退出交互模式的shell，应键入 （exit）
分析以下脚本代码: #!/bin/sh var="hello Linux" echo "var"echo′var"echo ′var' exit 0 从以下选项中选出正确的执行结果。 （hello Linux$var）
如何在bash中增加注释文字? （以#领头，直到行尾的文字都视为注释）
命令“2>&1”表示的含义是? （将标准错误输出重定向到标准输出）
在引用变量的内容时，应该在变量前使用符号 （$）
在shell脚本中可使用（read）命令把用户从键盘输入的数据读入到一个shell变量中。
23.若以普通用户进入Shell，则命令提示符为（$）22.在shell脚本中可使用（read）命令把用户从键盘输入的数据读入到一个shell变量中。23.若以普通用户进入Shell，则命令提示符为（） ;若以管理员身份进入Shell，则命令提示符为 （#） 。
24. 将前一个命令的标准输出作为后一个命令的标准输入，称之为 （管道） 。
判断题（共5题，10分）
Shell最重要的功能是命令解释器，Linux系统中的所有可执行文件都可以作为Shell命令来执行。 （对）
执行脚本的方法有两种，一种是赋予该脚本执行权限，直接执行脚本;另一种是将该脚本文件作为一个参数，通过shell解释器对其进行解析。 （对）
使用“&”连接符连接的命令，其前后命令的执行遵循逻辑与关系，只有该连接符之前的命令执行成功时，后面的命令才会被执行。 （错）
Shell脚本编程中，在使用echo命令打印变量时，使用单引号或双引号引起变量，对打印结果没有影响。 （错）
Shell脚本编程中，获取命令行输入的命令是read。 （对）